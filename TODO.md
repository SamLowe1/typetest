# Feature plans:
- Saving custom themes and handling of color collision with defaults (red for incorrect etc.)
- Custom wordbanks
- Funny fx as you type
- Saving scores/history

# TODO
- Maybe turn words and time based tests into subclasses of Test rather than Test having a mode attribute
- Make menu loop more consistent, just improve it in general
- Pull some things out of UI as it is a bit too big
- Make the top bar prettier as the dashes kind of stand out
- When playing in timed mode, either generate way more words to begin with,
 or add dynamic word generation.
- Fix theme support to extend to the whole terminal window
 (HINT: use some kind of fullscreen paint function to set the background when switching scenes or themes or something) -- DONE
